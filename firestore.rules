rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /ghostview_users/{userId} {
      allow read, write: if request.auth != null;
    }
    match /ghostview_messages/{messageId} {
      allow read, write: if request.auth != null;
    }
  }
}
